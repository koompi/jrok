export const VERSION = "3.0.0";
export const DEFAULT_SERVER = "https://tunnel.koompi.cloud";

// The GitHub repository was NOT renamed during the kproxy rebrand — it is still
// `koompi/jrok`. Used for the self-update check and install instructions.
export const GITHUB_REPO = "koompi/jrok";
export const GITHUB_API = `https://api.github.com/repos/${GITHUB_REPO}`;
export const GITHUB_RAW = `https://raw.githubusercontent.com/${GITHUB_REPO}`;
